# Telegram Bot
TELEGRAM_BOT_TOKEN=

# Naver Search API
NAVER_CLIENT_ID=
NAVER_CLIENT_SECRET=

# API 키 암호화 (Fernet)
# 생성: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# DB 경로 (기본값: data/tasa-check.db)
DB_PATH=/opt/tasa-check/data/tasa-check.db

# Langfuse (선택, 트레이싱용)
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_SECRET_KEY=
# LANGFUSE_HOST=https://cloud.langfuse.com
